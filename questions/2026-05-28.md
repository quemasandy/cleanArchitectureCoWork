¿Por qué hago la validación de un uuid usando un Value Object de UUID en la capa de aplicación y no lo puedo hacer en el controlador, en la capa de transporte?

---

¿Cuál es el lugar correcto para usar el Value Object UUID? ¿La capa de controlador o la capa de aplicación? 

---

Si tengo un Value Object de UUID que valida si es UUID, ¿dónde tiene que ir instanciado por primera vez ese Value Object? ¿En el controlador o en la capa de aplicación? 

---

O sea cada vez que yo creo una clase, como en este caso en específico en el que estoy creando el value object, la clase UUID, lo que estoy creando es un tipo de dato. Una clase es un tipo de dato ?

---

Ahora hay dos opciones:
1. Dejar que la base de datos genere el identificador de cada curso, el UUID.
2. Dejar que los clientes le pasen al backend el identificador único de cada curso.
¿Qué beneficio tiene que los clientes le pasen al backend el identificador de cada curso? ¿Por qué eso es mejor que dejar que la infraestructura de la base de datos genere el UUID?

---

¿Por qué es importante en el ValueObject en la clase UUID declarar un método toString? 

---

¿Qué es un UUID? 

---

por qué es mejor usar un inline type ? 
cuando usar interface ? vs cuando usar type ?
El instructor del video dice que usar type en TypeScript tiene un overhead en el tiempo de compilación. Consume demasiados recursos. 
¿Opina que reutilizar un mismo type a lo largo de todo el código es algo a evitar? No aclara por qué. ¿Estás de acuerdo? ¿Reutilizar un type a lo largo de todo el código no sería más mantenible? ¿Por qué? ¿Piensas que el instructor recomienda evitar usar o reutilizar un type a lo largo de todo el código? 
